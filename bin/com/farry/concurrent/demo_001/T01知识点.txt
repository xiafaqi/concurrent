T01知识点

1.synchronized关键字锁定的是对象不是代码块，T01中锁的是object对象的实例;
2.锁定对象有两种情况：a.类的实例；b.类的字节码(.class);
3.关于线程安全：加synchronized关键字之后不一定能实现线程安全，具体还要看
  锁定的对象是否唯一。T01中如果开启一个线程进行访问时是线程安全的，但如果
  开启不同的两条线程同时访问就会出现线程不安全的问题。